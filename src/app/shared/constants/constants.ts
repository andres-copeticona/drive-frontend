export enum ACCESS_TYPES {
  PUBLIC = 'publico',
  PRIVATE = 'privado',
  RESTRICTED = 'restringido',
  SHARED = 'compartido'
}

export enum FILE_CATEGORY {
  NEW = 'Nuevo',
  SIGNED = 'Sellado',
}

export enum SORT_DIR {
  ASC = 'asc',
  DESC = 'desc',
}
